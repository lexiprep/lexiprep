{
  "name": "lexiprep",
  "version": "0.0.0",
  "private": true,
  "description": "lexiprep app — server + web UI for the pre-reading vocabulary extractor. Consumes @lexiprep/core (separate repo).",
  "license": "AGPL-3.0-or-later",
  "author": "Valeriu Gutu <valeriu.manifest@gmail.com>",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.1.1",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
